(function ($, Drupal) {

	//



	// scroll effects
	$(window).scroll(function() {

		//

	});


	// resize effects
	$(window).resize(function() {

		//

	});

	$(document).keyup(function(e) {
		if (e.keyCode == 27) {
			modal_close();
		}
	});


})(jQuery, Drupal);
